
// javascript 
var minhaVar = 'Hello World';

function minhaFuncaoInicial(x, y) {
    return x + y;
}

// Ecmascript

let num = 2;
const PI = 3.14;

var numeros = [1, 2, 3]
numeros.map(function(valor) {
    return valor * 2;
});

numeros.map(valor => valor * 2); // ES 2015

class Matematica {
    soma(x, y){
        return x + y;
    }
}

function funcaoBasic(x, y){
    var z = 2;
    
    return 1;
}

